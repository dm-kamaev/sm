var Enum = require('../../../../api/components/enum');

module.exports = new Enum('pageName', {
    HOME: 'home',
    SEARCH: 'search',
    INFORMATION: 'information',
    ERROR_NOT_FOUND: 'error-not-found'
});
