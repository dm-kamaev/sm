{namespace sm.lSchool.bDataBlockFeatures.Template}
/**
* @param params
* @extends sm.lSchool.bDataBlock.Template
* @constructor
*/
{template .base}
    {call .init}
        {param params: [
            'header': $params.header,
            'headerType': $params.headerType ?
                        $params.headerType :
                        2,
            'modifier': $params.modifier ?
                        'b-data-block_features' + $params.modifier :
                        'b-data-block_features',
            'content': $params.content
        ] /}
    {/call}
{/template}
/**
* @param params
*/
{template .content autoescape="strict" kind="html"}
    <ul class="b-data-block__content">
    {foreach $feature in $params.content}
        <li class="b-data-block__item">
            <div class="b-data-block__dash">—</div>{$feature}
        </li>
    {/foreach}
{/template}
/**
* @param params
*/
{template .init autoescape="strict" kind="html"}
    <div class="b-data-block
        {if $params.modifier}
            {sp}{$params.modifier}
        {/if}
    ">

        //Header section
        {if $params.header}
            {call .header}
                {param params: [
                    'header': $params.header
                ] /}
            {/call}
        {/if}

        //Content  section
        {if $params.content}
            {call .content data="all" /}
        {/if}
    </div>
{/template}
/**
* @param params
*/
{template .header autoescape="strict" kind="html"}
    <div class="b-data-block__line"></div>
    {if $params.headerType == 2}
        <h2 class="b-data-block__header">{$params.header}</h2>
    {else}
        <h3 class="b-data-block__header">{$params.header}</h3>
    {/if}
{/template}
